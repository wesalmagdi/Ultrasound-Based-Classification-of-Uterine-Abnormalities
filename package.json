{
  "name": "uterine-ai-project",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "client": "cd client && npm start",
    "server": "cd server && node server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "child_process": "^1.0.2",
    "concurrently": "^8.2.0",
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "multer": "^2.0.2"
  }
}
